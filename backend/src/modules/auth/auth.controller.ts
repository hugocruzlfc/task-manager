import { Controller } from '@nestjs/common';

/**
 * AuthController — placeholder.
 * All auth routes (/api/auth/*) are handled by better-auth
 * mounted as Express middleware in main.ts.
 */
@Controller('auth')
export class AuthController {}
